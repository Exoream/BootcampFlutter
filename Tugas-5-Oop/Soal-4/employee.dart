class Employee {
  String? id;
  String? name;
  String? department;

  // Cara Pertama
  Employee.id(this.id);
  Employee.name(this.name);
  Employee.departement(this.department);

  // Cara Kedua
  Employee(this.id, this.name, this.department);
}
